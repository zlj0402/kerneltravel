/*
 * @brief: both heap way and quick sort way are finised. -- 28/4/2025
 *
 * @description:
 * 		+ heap ways 有两种方式:
 * 			+ 首先两种方式都支持大小顶堆;
 * 			+ way 1: Heap Way1(pop & re-insert)
 * 			+ way 2: Heap Way2(heap capacity: K, traverse & insert all)
 * 		+ class Heap 为了同时支持了，两种方式，略显臃肿; 用 struct 方式应该可以更简单;
 */
